{
  "name": "polish",
  "version": "0.1.0",
  "description": "Design/usability audit CLI for UI code. Layered rubric-based LLM review with weighted scoring.",
  "type": "module",
  "bin": {
    "polish": "./bin/polish.js"
  },
  "scripts": {
    "test": "node --test",
    "mcp": "node mcp-server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  }
}
